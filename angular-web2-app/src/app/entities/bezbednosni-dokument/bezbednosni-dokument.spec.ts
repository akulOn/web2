import { BezbednosniDokument } from './bezbednosni-dokument';

describe('BezbednosniDokument', () => {
  it('should create an instance', () => {
    expect(new BezbednosniDokument()).toBeTruthy();
  });
});
